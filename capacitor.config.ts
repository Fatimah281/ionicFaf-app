import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.ionicFaf',
  appName: 'ionicFaf',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
